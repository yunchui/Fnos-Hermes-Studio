# Hermes Agent 上游 release 标签（由 .github/workflows/auto-update.yml 自动跟踪更新）
# ensure_hermes_agent_src() 会按此标签下载离线源码包，保证构建可复现、与上游发版对齐。
# 初始值取自 NousResearch/hermes-agent 最新 release 标签（2026-07-20 核对）。
HERMES_AGENT_VERSION=v2026.8.18
